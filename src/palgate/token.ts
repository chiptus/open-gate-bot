// AES-128 + PalGate temporal-token derivation.
// Ported from https://github.com/RoeiOfri/homebridge-palgate-opener/blob/master/token_generator.js
// which is itself a port of https://github.com/DonutByte/pylgate. GPL-3.0.

const S_BOX = new Uint8Array([
  ,
]);

const INVERSE_S_BOX = new Uint8Array([
  ,
]);

const RCON = [0x01, 0x02, 0x04, 0x08, 0x10, 0x20, 0x40, 0x80, 0x1b, 0x36];

const BLOCK_SIZE = 16;
const KEY_SIZE = 16;

const T_C_KEY = new Uint8Array([
  0xfa, 0xd3, 0x25, 0x72, 0x81, 0x29, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00, 0x3a,
  0xb4, 0x5a, 0x65,
]);

const TOKEN_SIZE = 23;
const TIMESTAMP_OFFSET = 2;

export const TokenType = {
  SMS: 0,
  PRIMARY: 1,
  SECONDARY: 2,
} as const;

export type TokenTypeValue = (typeof TokenType)[keyof typeof TokenType];

function galoisMul2(value: number): number {
  return value & 0x80 ? ((value << 1) ^ 0x1b) & 0xff : (value << 1) & 0xff;
}

function bytesToHex(bytes: Uint8Array): string {
  let s = "";
  for (const b of bytes) s += b.toString(16).padStart(2, "0");
  return s;
}

function packUint64BE(num: number | bigint): Uint8Array {
  const result = new Uint8Array(8);
  let big = BigInt(num);
  for (let i = 7; i >= 0; i--) {
    result[i] = Number(big & 0xffn);
    big >>= 8n;
  }
  return result;
}

function aesEncryptDecrypt(
  stateBytes: Uint8Array,
  keyBytes: Uint8Array,
  isEncrypt: boolean,
): Uint8Array {
  if (stateBytes.length !== BLOCK_SIZE || keyBytes.length !== KEY_SIZE) {
    throw new Error("State and/or key are not 16 bytes");
  }
  const state = new Uint8Array(stateBytes);
  const key = new Uint8Array(keyBytes);
  _aesEncDec(state, key, isEncrypt);
  return state;
}

function _aesEncDec(
  state: Uint8Array,
  key: Uint8Array,
  encrypt: boolean,
): void {
  if (encrypt) {
    for (let rnd = 0; rnd < 10; rnd++) {
      key[0] = (S_BOX[key[13]!]! ^ key[0]! ^ RCON[rnd]!) & 0xff;
      key[1] = (S_BOX[key[14]!]! ^ key[1]!) & 0xff;
      key[2] = (S_BOX[key[15]!]! ^ key[2]!) & 0xff;
      key[3] = (S_BOX[key[12]!]! ^ key[3]!) & 0xff;
      for (let i = 4; i < KEY_SIZE; i++) {
        key[i] = (key[i]! ^ key[i - 4]!) & 0xff;
      }
    }
    for (let i = 0; i < BLOCK_SIZE; i++) {
      state[i] = (state[i]! ^ key[i]!) & 0xff;
    }
  }

  for (let rnd = 0; rnd < 10; rnd++) {
    if (encrypt) {
      for (let i = KEY_SIZE - 1; i > 3; i--) {
        key[i] = (key[i]! ^ key[i - 4]!) & 0xff;
      }
      key[0] = (S_BOX[key[13]!]! ^ key[0]! ^ RCON[9 - rnd]!) & 0xff;
      key[1] = (S_BOX[key[14]!]! ^ key[1]!) & 0xff;
      key[2] = (S_BOX[key[15]!]! ^ key[2]!) & 0xff;
      key[3] = (S_BOX[key[12]!]! ^ key[3]!) & 0xff;
    } else {
      for (let i = 0; i < BLOCK_SIZE; i++) {
        state[i] = S_BOX[state[i]! ^ key[i]!]!;
      }
      const buf1 = state[1]!;
      state[1] = state[5]!;
      state[5] = state[9]!;
      state[9] = state[13]!;
      state[13] = buf1;

      const tmp1 = state[2]!;
      const tmp2 = state[6]!;
      state[2] = state[10]!;
      state[6] = state[14]!;
      state[10] = tmp1;
      state[14] = tmp2;

      const tmp = state[15]!;
      state[15] = state[11]!;
      state[11] = state[7]!;
      state[7] = state[3]!;
      state[3] = tmp;
    }

    if ((rnd > 0 && encrypt) || (!encrypt && rnd < 9)) {
      for (let i = 0; i < 4; i++) {
        const base = i * 4;
        if (encrypt) {
          const b1 = galoisMul2(galoisMul2(state[base]! ^ state[base + 2]!));
          const b2 = galoisMul2(
            galoisMul2(state[base + 1]! ^ state[base + 3]!),
          );
          state[base] = (state[base]! ^ b1) & 0xff;
          state[base + 1] = (state[base + 1]! ^ b2) & 0xff;
          state[base + 2] = (state[base + 2]! ^ b1) & 0xff;
          state[base + 3] = (state[base + 3]! ^ b2) & 0xff;
        }
        const mix =
          state[base]! ^ state[base + 1]! ^ state[base + 2]! ^ state[base + 3]!;
        const first = state[base]!;
        let buf3 = galoisMul2(state[base]! ^ state[base + 1]!);
        state[base] = (state[base]! ^ buf3 ^ mix) & 0xff;
        buf3 = galoisMul2(state[base + 1]! ^ state[base + 2]!);
        state[base + 1] = (state[base + 1]! ^ buf3 ^ mix) & 0xff;
        buf3 = galoisMul2(state[base + 2]! ^ state[base + 3]!);
        state[base + 2] = (state[base + 2]! ^ buf3 ^ mix) & 0xff;
        buf3 = galoisMul2(state[base + 3]! ^ first);
        state[base + 3] = (state[base + 3]! ^ buf3 ^ mix) & 0xff;
      }
    }

    if (encrypt) {
      const buf1 = state[13]!;
      state[13] = state[9]!;
      state[9] = state[5]!;
      state[5] = state[1]!;
      state[1] = buf1;

      const tmp1 = state[10]!;
      const tmp2 = state[14]!;
      state[10] = state[2]!;
      state[14] = state[6]!;
      state[2] = tmp1;
      state[6] = tmp2;

      const tmp = state[3]!;
      state[3] = state[7]!;
      state[7] = state[11]!;
      state[11] = state[15]!;
      state[15] = tmp;

      for (let i = 0; i < BLOCK_SIZE; i++) {
        state[i] = (INVERSE_S_BOX[state[i]!]! ^ key[i]!) & 0xff;
      }
    } else {
      key[0] = (S_BOX[key[13]!]! ^ key[0]! ^ RCON[rnd]!) & 0xff;
      key[1] = (S_BOX[key[14]!]! ^ key[1]!) & 0xff;
      key[2] = (S_BOX[key[15]!]! ^ key[2]!) & 0xff;
      key[3] = (S_BOX[key[12]!]! ^ key[3]!) & 0xff;
      for (let i = 4; i < KEY_SIZE; i++) {
        key[i] = (key[i]! ^ key[i - 4]!) & 0xff;
      }
    }
  }

  if (!encrypt) {
    for (let i = 0; i < BLOCK_SIZE; i++) {
      state[i] = (state[i]! ^ key[i]!) & 0xff;
    }
  }
}

function step1(sessionToken: Uint8Array, phoneNumber: number): Uint8Array {
  const key = new Uint8Array(T_C_KEY);
  const phonePacked = packUint64BE(phoneNumber);
  for (let i = 0; i < 6; i++) {
    key[6 + i] = phonePacked[2 + i]!;
  }
  return aesEncryptDecrypt(sessionToken, key, true);
}

function step2(
  step1Result: Uint8Array,
  timestampSec: number,
  offset: number,
): Uint8Array {
  const next = new Uint8Array(BLOCK_SIZE);
  const val16 = 0xa0a;
  next[1] = val16 & 0xff;
  next[2] = (val16 >> 8) & 0xff;
  const val32 = timestampSec + offset;
  next[10] = (val32 >> 24) & 0xff;
  next[11] = (val32 >> 16) & 0xff;
  next[12] = (val32 >> 8) & 0xff;
  next[13] = val32 & 0xff;
  return aesEncryptDecrypt(next, step1Result, false);
}

export function hexToBytes(hex: string): Uint8Array {
  if (hex.length % 2 !== 0) throw new Error("Invalid hex string");
  const out = new Uint8Array(hex.length / 2);
  for (let i = 0; i < hex.length; i += 2) {
    out[i / 2] = parseInt(hex.slice(i, i + 2), 16);
  }
  return out;
}

export function generateTemporalToken(
  sessionTokenHex: string,
  phoneNumber: number,
  tokenType: TokenTypeValue,
  timestampSec: number = Math.floor(Date.now() / 1000),
): string {
  const sessionToken = hexToBytes(sessionTokenHex);
  if (sessionToken.length !== BLOCK_SIZE)
    throw new Error("Invalid session token");

  const s1 = step1(sessionToken, phoneNumber);
  const s2 = step2(s1, timestampSec, TIMESTAMP_OFFSET);

  const result = new Uint8Array(TOKEN_SIZE);
  if (tokenType === TokenType.SMS) result[0] = 0x01;
  else if (tokenType === TokenType.PRIMARY) result[0] = 0x11;
  else if (tokenType === TokenType.SECONDARY) result[0] = 0x21;
  else throw new Error(`unknown token type: ${tokenType}`);

  const phonePacked = packUint64BE(phoneNumber);
  result.set(phonePacked.slice(2, 8), 1);
  result.set(s2, 7);

  return bytesToHex(result).toUpperCase();
}
